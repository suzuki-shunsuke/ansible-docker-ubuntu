---
- hosts: default
  roles:
  # - ansible-docker-ubuntu
  - { role: ansible-docker-ubuntu, docker_users: vagrant}
- hosts: default
  tasks:
  - command: docker --version
    register: result
    # become: yes
  - debug: var=result
